{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Go client Spark Connect (localhost:15003)",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}",
        "env": { "SPARK_CONNECT_REMOTE": "sc://localhost:15003" }
      }
    ]
  }